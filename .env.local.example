# Local dev usually does not need these because Vite proxy serves `/api`.
# Uncomment only if you explicitly want to bypass proxy and hit a custom backend.
#
# VITE_API_URL=http://localhost:5000/api
# VITE_ENQUIRY_API_URL=http://localhost:5000/api
